/// A collection of Particles making up a rigid or soft Body
pub enum Body {
    Rect { width: f64, height: f64 },
    // hand draw shapes?
}

impl Body {
    fn push_to_vec() {
        todo!();
    }
}
